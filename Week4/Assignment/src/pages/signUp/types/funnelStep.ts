export type idInput = { id?: string; password?: string; nickname?: string };

export type passwordInput = {
  id: string;
  password?: string;
  nickname?: string;
};

export type nicknameInput = { id: string; password: string; nickname?: string };

export type signupComplete = { id: string; password: string; nickname: string };
